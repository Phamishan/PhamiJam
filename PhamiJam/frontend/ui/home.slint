import { Button, VerticalBox, HorizontalBox, ProgressIndicator, GridBox, LineEdit } from "std-widgets.slint";
import { globalVariables, Divider, HomeBtn, ProfileBtn, LibraryBtn, RecentPlaylistBtn, Interface } from "common.slint";

export component Home inherits Window {
    GridLayout {
        Row {
            Interface {
                GridLayout {
                    padding-left: 150px;
                    padding-bottom: 80px;
                    Rectangle {
                        GridBox {
                            Rectangle {
                                HorizontalBox {

                                    Image {
                                        width: 25px;
                                        source: @image-url(".././assets/search.png");
                                    }

                                    LineEdit {
                                        placeholder-text: "Search...";
                                    }
                                }
                            }

                            Rectangle {
                                GridBox {
                                    Rectangle {
                                        //background: green;
                                        row: 0;
                                        col: 0;
                                    }

                                    Rectangle {
                                        //background: pink;
                                        row: 0;
                                        col: 1;
                                    }

                                    Rectangle {
                                        //background: red;
                                        row: 0;
                                        col: 2;
                                    }

                                    Rectangle {
                                        //background: blue;
                                        row: 1;
                                        col: 0;
                                    }

                                    Rectangle {
                                        //background: black;
                                        row: 1;
                                        col: 1;
                                    }

                                    Rectangle {
                                        //background: purple;
                                        row: 1;
                                        col: 2;
                                    }
                                }

                                background: Colors.rgba(255, 255, 255, 0.2);
                                border-radius: 10px;
                                row: 1;
                                col: 0;
                            }
                        }
                    }
                }
            }
        }
    }
}
